// UI label for blocking grid
export const BLOCKING_GRID_NAME = 'Blocking Grid';

// UI label for boundary
export const BOUNDARY_NAME = 'Boundary';

// separate floor's z-coordinate by 1200 unit length
// TODO(we can make this adjustable in the future)
export const FLOOR_HEIGHT = 1200;

// Move original image to the back by 20 units to allow boundary to be
// fully displayed
export const IMAGE_PADDING = -40;

// the length of one 3D coordinates (x, y, z) in the coordinate lookup float
// array
export const ONE_POINT = 3;

// the number of points to create a triangle, used to loop and triangulate
export const ONE_TRIANGLE = 3;

// the length of two 3D coordinates (x, y, z) in the coordinate lookup float
// array
export const TWO_POINTS = 6;
