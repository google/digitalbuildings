SS:
  id: "14305791556165042176"
  description: "Basic combination of run command and status (start/stop) for an arbitrary device."
  is_abstract: true
  uses:
  - run_command
  - run_status

WDT:
  description: "Temperature differential across water."
  is_abstract: true
  uses:
  - return_water_temperature_sensor
  - supply_water_temperature_sensor