# DigitalBuildings: (ontology and toolset)
# currently being used by Google internally to manage our own buildings.
package(default_visibility = ["//visibility:public"])

licenses(["notice"])

exports_files(["LICENSE"])
